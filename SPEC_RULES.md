# SPEC_RULES.md — 명세 작업 규칙

> 이 문서를 읽는 Claude에게: 당신은 이 작업의 앞선 대화를 기억하지 못합니다.
> 이 문서가 유일한 지침입니다. 작업 전 전문을 읽고, 매 세션 시작 시 다시 읽으십시오.

---

## 1. 이 작업은 무엇인가

INDAIM(Gemini 기반 한국어 TRPG 디스코드 봇)의 **전 기능 명세를 코드에서 역추출**합니다.

산출물은 `specs/` 아래 영역별 명세 문서입니다.

### 왜 하는가

이 프로젝트는 5,000줄 규모일 때 사용자가 모든 내용을 인지하고 **유기적 설계로 요소요소를 의미 있게** 만들었습니다. 지금은 25,000줄이 넘어 Claude가 그 연결을 다 파악하지 못합니다.

실제로 다음 사고가 났습니다.

| 사고 | 원인 |
|---|---|
| 퀘스트가 시작되지 않음 | 필터·주입·진행은 있는데 `start_quest` 호출부가 없었다 |
| 세션을 열어도 GM이 반응 없음 | `gm_active`가 명령어 경로에만 있었다 |
| NPC 이미지 전부 미출력 | 미디어 폴더명이 시나리오 id와 달랐다 |
| 잘못된 이름으로 통일 | 데이터가 많은 쪽을 원본으로 착각했다 |

**전부 "이것이 왜 있는지, 무엇과 연결되는지"를 몰라서 생긴 일입니다.**

### 이 명세로 무엇을 할 것인가

명세가 완성되면 대규모 리팩터링 또는 재개발의 **기준 문서**가 됩니다. 지금은 어느 쪽이 옳은지 판단할 근거가 없습니다.

따라서 명세는 **"코드가 이렇게 생겼다"가 아니라 "이 기능이 무엇을 위해 존재하고 무엇에 의존하는가"**를 담아야 합니다.

---

## 2. 절대 규칙

### ① 추측을 사실로 쓰지 않는다

코드에서 읽어낸 것과 추론한 것을 반드시 구분합니다.

```markdown
동행은 명시적으로 끝나기 전까지 유지된다. `[코드]`
이는 대화만 나눈 인물과 함께 이동하는 인물을 구분하기 위함으로 보인다. `[추정]`
왜 상주 NPC만 자동 해제하는지는 알 수 없다. `[확인 필요]`
```

| 표기 | 의미 |
|---|---|
| `[코드]` | 코드·주석·커밋 메시지에 명시된 것 |
| `[추정]` | Claude가 문맥에서 추론한 것 |
| `[확인 필요]` | 의도를 알 수 없어 사용자 확인이 필요한 것 |

**`[확인 필요]`를 두려워하지 마십시오.** 모르는 것을 아는 척 쓰는 편이 훨씬 위험합니다. 사용자는 이것을 보고 의도를 알려줄 것입니다.

### ② 코드를 직접 읽고 쓴다

`grep` 결과만으로 쓰지 않습니다. 해당 함수를 열어 본문을 읽고 씁니다.

이 프로젝트에서 반복된 실패가 **"검출 결과를 확인 없이 보고"**입니다. `refund_ink`를 미사용으로 판정했으나 `as _refund` 별칭으로 쓰이고 있었습니다.

### ③ 연결을 반드시 추적한다

기능 하나를 쓸 때 다음을 **모두** 확인합니다.

```
누가 호출하는가         호출부가 없으면 그 사실을 명시 (죽은 코드일 수 있다)
무엇을 호출하는가       하위 의존
어떤 데이터를 읽는가     세션 필드·시나리오 JSON 키
어떤 데이터를 쓰는가     부작용
언제 실행되는가         턴 진행 중? 세션 생성 시? 백그라운드?
실패하면 어떻게 되는가   조용히 넘어가는가, 전파되는가
```

**호출부 없는 기능을 발견하면 반드시 표시**합니다. 이것이 이 작업의 가장 큰 수확이 될 수 있습니다.

### ④ 원본을 수정하지 않는다

명세 작업 중에는 **코드·시나리오·프롬프트를 일절 건드리지 않습니다.** 버그를 발견해도 명세에 기록만 하고 넘어갑니다.

```markdown
> ⚠️ **결함 의심** — `foo()`가 `bar`를 검사하지 않아 …할 수 있다. `[확인 필요]`
```

수정은 명세 완료 후 별도로 논의합니다. 작업 중 코드가 바뀌면 이미 쓴 명세가 어긋납니다.

### ⑤ 분량을 아끼지 않는다

사용자 지시입니다 — *"분량을 아끼지 말고, 토큰을 아끼지 말고, 시간을 아끼지 말며, 자원을 아끼지 마십시오."*

기능 하나에 한 문단이 필요하면 한 문단을 씁니다. 목록으로 압축해 의미가 사라지면 안 됩니다.

**다만 반복은 피합니다.** 같은 설명을 두 영역에 쓰지 말고 한쪽에서 참조합니다.

---

## 3. 영역 구획

`core/` 45개 모듈을 10개 영역으로 나눕니다. `cogs/`는 해당 영역에 붙입니다.

| # | 영역 | 모듈 | 줄 | cogs |
|---|---|---|---|---|
| 1 | **base** | constants · models · io · utils | 1,101 | — |
| 2 | **ai** | prompt · extraction · dialogue · resilience | 1,461 | gm |
| 3 | **session** | session_flow · creation · session_open · gm_space · intro | 1,699 | session |
| 4 | **profile** | profile_gen · profile_runner · profile_creation_ui · profile_ai · profiles · profile_ui | 2,455 | character |
| 5 | **world** | places · timeline · start_frame · irregular_npc · growth · koreantext | 1,413 | — |
| 6 | **quest** | quest · quest_filter | 1,075 | gm(퀘스트 명령) |
| 7 | **cost** | cost · estimate · ink · accounts · terms · stats | 1,620 | system |
| 8 | **memory** | memory_plan · rewind · cache | 1,073 | game |
| 9 | **media** | audio_mixer · tts · tts_preset · media · media_control | 1,092 | media |
| 10 | **ui** | display · ui · chat_guard | 998 | — |

### 작업 순서

**base → ai → world → quest → session → profile → cost → memory → media → ui**

아래에서 위로 쌓습니다. `base`를 먼저 해야 다른 영역이 참조할 수 있고, `ai`를 알아야 층위 흐름이 이해됩니다.

한 세션에 **한 영역**을 목표로 합니다. 큰 영역(profile 2,455줄)은 나눠도 됩니다.

---

## 4. 문서 구조

각 영역은 `specs/{번호}_{영역}.md`로 씁니다. 다음 구조를 따릅니다.

````markdown
# {영역명} — 기능 명세

- **모듈**: core/xxx.py · core/yyy.py
- **연관 cogs**: cogs/zzz.py
- **기준 버전**: v5.29.1
- **작성 상태**: 완료 / 진행 중 / 미검증

---

## 개요

이 영역이 무엇을 담당하는지 3~5문장. 다른 영역과의 경계를 밝힌다.

## 설계 의도

**왜 이렇게 만들었는가.** 코드 주석·커밋 메시지·DEVLOG에서 근거를 찾아 쓴다.
근거가 없으면 `[추정]` 또는 `[확인 필요]`로 표시한다.

## 데이터 구조

이 영역이 정의하거나 소비하는 데이터.

### 세션 필드
| 필드 | 타입 | 기본값 | 용도 | 되감기 |
|---|---|---|---|---|

### 시나리오 JSON 키
| 키 | 타입 | 필수 | 용도 | 없으면 |
|---|---|---|---|---|

### 파일
경로와 형식.

## 기능 목록

각 기능을 다음 형식으로 쓴다.

### `function_name(args) -> ret`

**무엇을 하는가** — 한두 문장.

**왜 있는가** — 이 기능이 없으면 무엇이 안 되는가. `[코드]` / `[추정]`

**호출부**
- `cogs/gm.py:1234` — 턴 진행 중
- (호출부 없음 → ⚠️ 표시)

**의존**
- 읽기: `session.xxx`, 시나리오 `yyy`
- 쓰기: `session.zzz`
- 호출: `other_func()`

**예외·실패**
실패 시 동작. 조용히 넘어가는지, 전파되는지.

**주의점**
있으면 쓴다. 없으면 생략.

## 흐름

이 영역의 기능들이 어떤 순서로 엮이는지. 도식이 유용하면 도식으로.

```
사용자 조작 → A() → B() → C()
                      ↓ 실패 시
                    D()
```

## 다른 영역과의 접점

| 상대 영역 | 방향 | 내용 |
|---|---|---|

## 발견 사항

작업 중 발견한 것. 결함 의심·죽은 코드·중복·불일치.

> ⚠️ **호출부 없음** — `foo()`가 정의만 되어 있다.
> ⚠️ **결함 의심** — …
> 🔁 **중복** — `bar()`와 `baz()`가 같은 일을 한다.

## 확인 필요 목록

사용자에게 물어야 할 것을 모아 둔다. 이 절이 비어 있으면 대개 조사가 부족한 것이다.

- [ ] `foo()`의 `strict` 인자는 어떤 상황을 위한 것입니까?
- [ ] `bar` 필드가 되감기 대상이 아닌 이유가 있습니까?
````

---

## 5. 작업 절차

### 세션 시작 시

1. `SPEC_RULES.md`(이 문서) 전문을 읽는다
2. `specs/00_INDEX.md`에서 진행 상황을 확인한다
3. `CLAUDE.md`로 프로젝트 전반을 파악한다
4. 이번에 쓸 영역의 이전 명세(있으면)를 읽는다

### 영역 하나를 쓸 때

```
① 해당 모듈 전문을 읽는다 (grep이 아니라 view/cat)
② 각 공개 함수의 호출부를 전 코드베이스에서 찾는다
③ 세션 필드·시나리오 키 사용처를 추적한다
④ 커밋 이력에서 그 기능이 왜 추가됐는지 찾는다
   git log --oneline -- core/xxx.py
   git log -S "function_name" --oneline
⑤ 명세를 쓴다
⑥ 확인 필요 목록을 정리한다
⑦ specs/00_INDEX.md를 갱신한다
⑧ 커밋한다
```

### 커밋 규칙

```
docs(spec): {영역명} 명세 작성

발견 사항 N건 · 확인 필요 M건

주요 발견:
- …
```

**한 영역 = 한 커밋**입니다. 중간에 세션이 끊겨도 다음 Claude가 이어받을 수 있습니다.

---

## 6. 함정 — 이 프로젝트에서 실제로 겪은 것들

### 검출 결과를 확인 없이 믿지 말 것

```
refund_ink 미사용으로 판정 → 실제로는 as _refund 별칭으로 사용 중
npcs 중복 정의로 판정      → __init__ 내 정상 초기화
```

**반드시 코드를 열어 확인**합니다.

### 이름이 실체와 다를 수 있다

```
MIN_CACHE_TTL     이름은 '최소'인데 실제로는 6시간 전체값
서사 설계자(방향성) 서사설계자와 무관한 별개 호출
```

### 데이터와 코드가 어긋날 수 있다

```
NPC 이름 '김철수' vs 이미지 '강철수.png'
media/shadow_island vs scenario_id '영도'
```

**어느 쪽이 원본인지 임의로 판단하지 말고 `[확인 필요]`로 표시**합니다.

### 같은 일을 여러 곳에서 한다

```
세션 저장 89회 / 13파일
AI 호출 18회 / 6파일 — 각자 7단계를 반복
```

중복을 발견하면 `🔁`로 표시하되, **어느 쪽이 원본인지 단정하지 않습니다.**

### 기획 의도가 코드에 안 남아 있을 수 있다

주석·커밋에 없으면 `[확인 필요]`입니다. 5,000줄 시절의 설계 의도는 사용자 머릿속에 있습니다.

---

## 7. 품질 기준

명세 하나를 끝냈을 때 다음을 자문합니다.

```
□ 이 문서만 읽고 해당 영역을 재구현할 수 있는가
□ 각 기능이 "왜 있는지" 답할 수 있는가
□ 호출부 없는 기능을 전부 찾아냈는가
□ 세션 필드·시나리오 키를 빠짐없이 적었는가
□ 다른 영역과의 접점을 양방향으로 적었는가
□ 추측과 사실을 구분해 표기했는가
□ 확인 필요 목록이 비어 있지 않은가
```

마지막 항목이 중요합니다. **모르는 것이 하나도 없다면 조사가 얕은 것**입니다.

---

## 8. 하지 말 것

| 금지 | 이유 |
|---|---|
| 코드·시나리오·프롬프트 수정 | 명세가 어긋난다. 발견만 기록 |
| 버전 올리기 | 기능 변경이 아니다 |
| 리팩터링 착수 | 명세 완료 후 논의 |
| 미사용 코드 제거 | 의도가 있을 수 있다 |
| 추측을 단정으로 쓰기 | 잘못된 명세는 없느니만 못하다 |
| 여러 영역 동시 작업 | 세션이 끊기면 전부 미완이 된다 |

---

## 9. 진행 관리

`specs/00_INDEX.md`에 다음을 유지합니다.

```markdown
# 명세 작업 진행

기준 버전: v5.29.1

| # | 영역 | 상태 | 발견 | 확인필요 | 커밋 |
|---|---|---|---|---|---|
| 1 | base | ✅ 완료 | 3 | 5 | abc1234 |
| 2 | ai | 🔄 진행 중 | - | - | - |
| 3 | session | ⬜ 대기 | - | - | - |

## 누적 확인 필요 목록
영역별 문서의 '확인 필요'를 여기에 모은다. 사용자가 한 번에 답할 수 있게.

## 누적 발견 사항
결함 의심·죽은 코드를 여기에 모은다.
```

---

## 10. 사용자와의 소통

- **확인 필요 항목은 영역 완료 시 한 번에** 제시합니다. 작업 도중 하나씩 묻지 않습니다.
- 답을 받으면 **명세를 갱신하고 재커밋**합니다.
- 사용자는 이 프로젝트의 총괄 디렉터입니다. 정중한 존대를 쓰고, Claude 자신은 '저'로 지칭합니다.
- 발견 사항 중 **당장 위험한 것**(데이터 손실·비용 폭증 가능성)은 즉시 보고합니다.
