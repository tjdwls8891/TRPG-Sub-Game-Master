# INDAIM — 개발 로그 (DEVLOG)

> 현재 버전: **v5.24.0** · `SCHEMA_VERSION` 3
> 스택: Gemini API + discord.py / 한국어 TRPG 보조 GM 봇
> 저장소: `tjdwls8891/TRPG-Sub-Game-Master`

---

## 목차

1. [설계 결정 기록](#설계-결정-기록)
2. [개발 이력](#개발-이력)
3. [실측 데이터](#실측-데이터)
4. [잔여 개발 계획](#잔여-개발-계획)
5. [반복된 실패 패턴](#반복된-실패-패턴)

---

## 설계 결정 기록

구현 세부는 `CLAUDE.md`에 있다. 여기에는 **왜 그렇게 했는가**를 남긴다.

### 4층위 분리 (v4.3.0)

단일 호출로 판단·묘사·정리를 모두 시키면 캐시를 매번 읽어 비용이 선형으로 증가한다. 층위를 나눠 **ASK·ROLL은 캐시를 읽지 않게** 했다.

| 유형 | 캐시 읽기 | 비고 |
|---|---|---|
| ASK / ROLL | 0회 | 판단층위에서 끝 |
| NARRATE / PROCEED | 2회 | 지시 + 묘사 |

되묻는 턴이 가장 저렴하다는 구조가 여기서 나온다.

**부작용** — 판단층위가 세계관을 모른다. 이동 안내(v5.8.0)처럼 세계 정보가 필요한 판단은 **코드가 계산해 사용자 프롬프트에 넣어야** 한다.

### 수치 판단 원칙 (v4.x 전반)

모델에게 "위험한가?"를 묻지 않는다. 0~100 점수를 받아 코드가 임계값과 비교한다.

같은 상황에서 같은 결과가 나와야 하고, 임계값을 시나리오별로 조정할 수 있어야 하기 때문이다. 캐시 시간 해석(v4.28.0)에서도 같은 구조를 썼다 — **분류는 모델, 수치는 코드**.

### 온디맨드 주입

장소·퀘스트·NPC 정보를 캐시에 구우면 시나리오가 커질수록 모든 턴의 비용이 오른다. 캐시에는 룰북(고정분)만 넣고, 나머지는 턴마다 필요한 만큼 주입한다.

키워드북 폐지로 비운 슬롯에 장소 정보(450~900토큰)가 들어갔다.

### 계층은 틀이 아니라 그래프 (v5.8.0)

`tier` 같은 고정 계층을 두면 시나리오가 그 틀에 맞춰져야 한다. 섬서 아래 화음(깊이 3)과 종남산(깊이 2)이 대등할 수 있어야 하고, 낄 계층이 없으면 없는 대로 둬야 한다.

`parent`만 두고 깊이는 결과로 나오게 했다. 최소단위는 하위 존재 여부로 자동 판정된다.

### 필터가 곧 슬롯 (v5.12.0)

퀘스트를 인스턴스로 저작하면 장소 122곳에 100종 이상이 필요하다. 필터에 후보를 나열해 두고 **일치한 값이 빈칸을 채우게** 하면 하나의 틀이 여러 곳에서 각기 다른 사건이 된다.

실제로 **15종으로 122곳 전부를 덮었다.**

개연성은 구조로 막는다 — NPC 후보를 현재 장소 실재 인물과 교집합 내므로, 중리 사람이 남항 창고에서 발동하는 일이 애초에 불가능하다. 경로 추정이 아니라 직접 대조라 무협처럼 다른 구조에서도 통한다.

### 만남과 동행의 분리 (v5.11.0)

`npcs_met`은 그 턴에 등장한 인물일 뿐이라 스쳐 지나간 사람과 함께 움직이는 사람이 구분되지 않는다. 퀘스트 인물 필터가 이를 못 가리면 **이미 헤어진 인물과의 사건**이 발동한다.

추출층위가 `joined`/`left` **변화만** 보고하게 한 것도 의도적이다. 목록 전체를 다시 쓰게 하면 모델이 기존 동행자를 누락시킨다.

### 능력치는 랜덤, 조건은 등급 (v5.6.x)

프리셋 방식(고르면 결정적 배분)은 기획 의도와 달랐다. **산출은 언제나 랜덤**이고 총합·편차·특화가 제약이다.

수치 입력 대신 별명(허접~먼치킨)으로 고르게 했고, **상한 대비 비율**로 환산해 스탯 개수가 다른 시나리오에서도 통하게 했다.

```
영도(3종 1~20)  허접15 약골21 평범30 튼튼37 능력자45 먼치킨54
무협(4종 1~20)  허접20 약골28 평범40 튼튼50 능력자60 먼치킨72
평균            5.0  7.0  10.0  12.3  15.0  18.0
```

직업 치중은 **최고값과 교환**한다. 재배분이 아니라 교환이라 총합·편차 제약을 깨지 않는다.

### 캐시 업로드를 프로필 뒤로 (v5.3.0)

채널 생성과 동시에 캐시를 올리면 플레이어가 캐릭터를 만들기도 전에 비용이 청구되고, 중도 이탈 시 그대로 손실이 된다. 유지 시간도 그 시점에는 답을 받지 못했다.

`build_session`을 `provision_session`(채널)과 `upload_cache`(캐시)로 분리했다.

---

## 개발 이력

### v1.0~v1.9 — 기반

단일 `core.py`(1,896줄)를 10개 서브모듈로 분리. Auto-GM 도입, 오디오 믹싱, TTS 더빙, `prompts.py` 분리.

### v4.3.0~v4.20.0 — 층위 분리와 세계 상태

| 버전 | 내용 |
|---|---|
| 4.3.0 | **4층위 분리** — 판단/지시/묘사/추출 |
| 4.16.0 | 이탈 수치 재계획 트리거, 시간선 정량화 |
| 4.17.0 | BGM 자동재생, 통계 서비스, 오류대응 래퍼 |
| 4.19.0 | 디스플레이 채널, `bgm_map` |
| 4.20.0 | 채팅 차단 단일 훅 |

### v4.21.0~v4.36.0 — 계정·프로필·퀘스트 기반

| 버전 | 내용 |
|---|---|
| 4.21.1 | TTS 사전 저장을 런타임 캐시 → 사전 생성으로 **재구현** |
| 4.22.0 | 기획 대조 — 비정규 NPC 배정, 세션 종류, BGM 페이드아웃 |
| 4.24.0 | 능력치 성장·행운 스탯 |
| 4.25.0 | 서버 GM 스페이스·명예의 전당 |
| 4.27.0 | 약관 동의·계정 등록 DM |
| 4.28.0 | 캐시 업로드 시간 선택제 |
| 4.29.0 | 세션 제작 상태 기계, 사전 저장 프로필 |
| 4.30.0~4.32.0 | 프로필 생성 모듈·실행부·AI |
| 4.33.0~4.35.0 | 퀘스트 시스템, 선택 조립, 이면정보 |
| 4.36.0 | 기억 압축 플랜 4종 |

### v5.0.0 — 세션 구조 개혁

`auto_gm_*` 14필드를 `gm_*`로 이관하고 **`SCHEMA_VERSION` 2→3**. `migrate_session_data`로 구세션을 자동 변환한다.

치환 순서에 함정이 있었다. `auto_gm_target_char`가 `auto_gm_target_chars`의 **접두**라, 단수형을 먼저 치환하면 복수형이 깨진다. 길이 내림차순을 코드로 강제했다.

### v5.1.0~v5.5.1 — 진입점 오케스트레이션

**4.29~4.38에서 만든 모듈들이 호출부 없이 방치돼 있었다.** 모듈·데이터·실행부는 있었으나 순서대로 부르는 코드가 없어 아무것도 작동하지 않았다.

| 버전 | 내용 |
|---|---|
| 5.1.0 | `build_session` 분리, 프로필 풀오토로 위저드 대체 |
| 5.2.0 | 사전 프로필 생성·게임 채널 참가 버튼 |
| 5.3.0 | **기획서 17단계 통합** — 캐시 업로드를 프로필 뒤로 |
| 5.3.1 | 음성 채널 참가자 인식 |
| 5.4.0 | 여러 턴 되감기 버튼 |
| 5.4.1 | 캐시 TTL·플레이 차단·업로드 비용 3건 수정 |
| 5.5.0 | 결제 계층 연결 — 지급·차감·오픈/클로즈 정산 |
| 5.5.1 | 미연결 기능 6건 결선 |

### v5.6.0~v5.10.0 — 프로필·장소

| 버전 | 내용 |
|---|---|
| 5.6.0 | 능력치를 프리셋에서 **제약 랜덤**으로 재구현 |
| 5.6.1~5.6.3 | 등급 별명, 범위 1~20, 무작위 총합 보정 |
| 5.7.0 | 슬롯 조립 결함 3종 — 조사·중복·맥락 |
| 5.8.0 | **장소 계층 시스템**, 이동 개연성 |
| 5.8.2 | 가시성 판정 `visible_within` |
| 5.9.0 | 퀘스트 장소 필터, `faction`/`faction_scope` 분리 |
| 5.9.1~5.9.3 | 장소 20 → 48 → 61 → 80개 |
| 5.10.0 | 직업 16종 개편, 시작 틀 재구성 |

### v5.11.0~v5.15.0 — 퀘스트 재편

| 버전 | 내용 |
|---|---|
| 5.11.0 | **동행 시스템** — 만남과 동행 분리 |
| 5.12.0 | **퀘스트 필터 매칭** — 필터가 곧 슬롯 |
| 5.12.1 | 초반군 6종 |
| 5.13.0 | 구 퀘스트 15종 제거 |
| 5.13.1~5.14.0 | 4세력 가입 16경로 |
| 5.14.1 | 장소 80 → 122개 |
| 5.15.0 | 장소군·인물군 15종, **커버리지 122/122** |

---

## 실측 데이터

### 비용 (26,268토큰 캐시 기준)

```
업로드만        19.70원
3시간 유지     137.91원
6시간 유지     256.11원
```

`estimate_session_open`과 실제 청구가 **같은 함수**를 쓴다. v5.4.1 이전에는 저장비를 따로 계산해 예상과 청구가 달랐다.

### 턴 비용

4층위 합계 ₩30.24 (실측). 개별 층위는 ₩11~15.

### 영도 이동 거리 (세계관 대조)

| 구간 | 홉 | 판정 | 세계관 |
|---|---|---|---|
| 남항↔해련 | 2 | 한 턴 | 40~60분 |
| 남항↔중리 | 2 | 한 턴 | 70~90분 |
| 해련↔중리 | 2 | 한 턴 | 50~70분 |
| 중리→조도 | 3 | 한 턴 | 해안로+방파제 |
| 해련→조도 | 5 | **ASK** | 최장 90~120분 |

### 데이터 규모

```
장소 122 · NPC 47(32명 배치) · 직업 16 · 퀘스트 44
시작 틀 8 · 프로필 단계 10 · SESSION_FIELDS 73 · TRACKED_PATHS 14
명령어 44 · persistent view 5종 22버튼
```

---

## 잔여 개발 계획

### 우선순위 높음

| 항목 | 내용 |
|---|---|
| **메인라인 퀘스트** | 구 15종 폐기 시 `고립된 낙원`이 사라졌다. 서브 클리어 조건을 채워도 갈 곳이 없다 |
| **소속군 퀘스트** | 가입 16경로는 있으나 가입 후 할 일이 없다 |
| **무협 시나리오** | `places`·`profile_creation`·퀘스트 전무. 문파·경지 체계라 별도 설계 필요 |

### 설계 보류

**난이도·대결 판정** — 두 문제가 미결이다.

① 무협의 `경지`처럼 능력치 외 항목이 고하를 결정하는 경우, 난이도를 절대값으로 두면 계층이 무의미해진다.
② 난이도는 세계관 맥락 없이 정할 수 없는데 **판단층위는 캐시를 읽지 않는다.**

제안된 해법 — 계층 항목을 시나리오가 정의(`tier_field`), 목표값을 `min(능력치, 계층 상한)`으로 절삭, 난이도는 모델이 정하지 않고 **행동 유형 분류**로 표에서 꺼낸다.

### 데이터 대기

| 항목 | 비고 |
|---|---|
| NPC `birth_year` | 0/47명. `enrich_npc_ages`가 대기 |
| 오디오 자산 | 공통 소개 TTS·테마곡 |
| `irregular_images` | 비정규 NPC 이미지 풀 |
| BGM 트랙 | |

### 미사용 유지 중

제거 후보였으나 **유지 결정**된 것들. 연결하면 살아난다.

```
profile_runner.jump_to      기획 필요기능 5번 (특정 단계만 호출). UI 버튼 없음
profile_gen.reroll_stats    UI가 cancel_pending으로 우회
ink.can_afford              max_ink 직접 비교로 대체됨
memory_plan.plan_key        get_plan과 중복
creation.is_done/get_data   상태 기계 API 완결성
```

---

## 반복된 실패 패턴

같은 실수를 반복하지 않기 위해 기록한다.

### ① 만들고 연결하지 않음

4.29~4.38의 모듈 전부가 호출부 없이 방치됐다. `creation.py`·`profile_runner.py`는 5.3.0까지 죽어 있었다.

**"완료"의 기준은 임포트가 아니라 플레이어의 도달 경로다.** 커밋 전 `grep`으로 진입점을 확인한다.

### ② 검출 결과를 확인 없이 보고

정규식이 뱉은 것을 그대로 문제로 옮긴 사례가 여럿이다.

- `npcs` 중복 정의 → `__init__` 내 정상 초기화였음
- 교차 cog 호출 3건 → 다른 cog의 메서드 정상 호출
- `refund_ink` 미사용 → `as _refund` 별칭으로 사용 중

**해당 코드를 열어 읽은 뒤에만 보고한다.**

### ③ 기획서를 다시 읽지 않고 익숙한 패턴으로 구현

- TTS 사전 저장을 런타임 캐시로 만듦 (4.21.1에서 재구현)
- 능력치를 프리셋으로 만듦 (5.6.0에서 재구현)
- 시작 상황이 소속을 전제 (5.10.0에서 재작성)

**구현 전 해당 기획 문구를 다시 확인한다.**

### ④ 파일 전체 교체 스크립트

5.12.1 저작 시 기존 15종을 통째로 덮었다. git에 있어 복구했으나, **`extend`가 아니라 대입**을 쓴 것이 원인이다.

### ⑤ 명칭을 임의로 지음

장소 저작 시 월드뷰의 정식 명칭(`흑색 작업장`·`바자르`·`지하 병참 창고`)을 두고 임의로 지었다. 5.9.2 교차검증에서 8곳을 교정했고, `다중 검문소`는 **소속 세력 자체가 틀렸다.**

**저작 전 월드뷰를 검색해 기존 명칭을 확인한다.**
